#include "recursohidrico.h"

// Constructor de la clase RecursoHidrico
RecursoHidrico::RecursoHidrico(const QString& nombre, Tipo tipo, const QString& descripcion,
                               const QString& datos, const QString& importancia)
    : nombre(nombre), tipo(tipo), descripcion(descripcion), datos(datos), importancia(importancia)
{
    // Inicializa las variables miembro con los parámetros proporcionados
}
